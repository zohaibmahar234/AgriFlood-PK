# Demo data
No synthetic flood polygon is shipped as scientific evidence.

Software tests create synthetic values only to verify code paths. The offline historical replay uses published UNOSAT metadata/statistics and explicitly sets `scientific_raster_included=false`.
